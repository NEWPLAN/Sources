#include <stdio.h>
#include <math.h>

int main(void)
{
    int nums;
    int i,j,k,p;
    while(scanf("%d",&nums)!=-1)
    {
        p=sqrt(nums);
        for(i=0;i<=p;i++)
        {
            for(j=0;j<=p;j++)
            {
                k=sqrt(
                       )
            }
        }
    }
    return 0;
}
